# GridTile Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Menu-bar macOS app. Ctrl+Opt+Cmd+T tiles all visible windows on the display under the mouse into an auto-sized grid.

**Architecture:** Pure layout math in a library target (`GridTileCore`, unit tested). Executable target enumerates on-screen windows with `CGWindowListCopyWindowInfo`, matches them to Accessibility (AX) windows by PID and frame, and sets AX position/size. Carbon `RegisterEventHotKey` for the global hotkey. `build.sh` wraps the binary into a `.app`.

**Tech Stack:** Swift 5.9, SwiftPM, AppKit, ApplicationServices (AX), Carbon (hotkey), ServiceManagement (login item), XCTest. macOS 13+.

Spec: `docs/superpowers/specs/2026-09-16-gridtile-design.md`. One deviation from the spec: `Grid.swift` lives in its own library target `GridTileCore` so tests link no AppKit. `Info.plist` lives at repo root.

---

## File structure

```
Package.swift
Info.plist
build.sh
Sources/GridTileCore/Grid.swift        # gridLayout(count:in:) pure function
Sources/GridTile/Windows.swift          # VisibleWindow, visibleWindows(on:), cgRect(of:), apply(frame:to:)
Sources/GridTile/Hotkey.swift           # Hotkey class wrapping Carbon RegisterEventHotKey
Sources/GridTile/main.swift             # tileNow(), AppDelegate, status item, app.run()
Tests/GridTileCoreTests/GridTests.swift
```

Coordinate rule: everything in `GridTile` and `GridTileCore` uses CG coordinates (origin top-left of primary display, y down). Only `cgRect(of:)` touches AppKit's bottom-left coordinates.

---

### Task 1: Package + grid layout (TDD)

**Files:**
- Create: `Package.swift`
- Create: `Sources/GridTileCore/Grid.swift`
- Create: `Tests/GridTileCoreTests/GridTests.swift`
- Create: `Sources/GridTile/main.swift` (placeholder so package builds; replaced in Task 3)
- Create: `.gitignore`

- [ ] **Step 1: Write Package.swift**

```swift
// swift-tools-version:5.9
import PackageDescription

let package = Package(
    name: "GridTile",
    platforms: [.macOS(.v13)],
    targets: [
        .target(name: "GridTileCore"),
        .executableTarget(name: "GridTile", dependencies: ["GridTileCore"]),
        .testTarget(name: "GridTileCoreTests", dependencies: ["GridTileCore"]),
    ]
)
```

- [ ] **Step 2: Write .gitignore and placeholder main**

`.gitignore`:
```
.build/
build/
*.xcodeproj
.DS_Store
```

`Sources/GridTile/main.swift`:
```swift
print("placeholder")
```

- [ ] **Step 3: Write the failing tests**

`Tests/GridTileCoreTests/GridTests.swift`:
```swift
import XCTest
@testable import GridTileCore

final class GridTests: XCTestCase {
    let area = CGRect(x: 0, y: 25, width: 1200, height: 800)

    func testZeroReturnsEmpty() {
        XCTAssertEqual(gridLayout(count: 0, in: area), [])
    }

    func testOneFillsArea() {
        XCTAssertEqual(gridLayout(count: 1, in: area), [area])
    }

    func testTwoIsSideBySide() {
        let cells = gridLayout(count: 2, in: area)
        XCTAssertEqual(cells, [
            CGRect(x: 0, y: 25, width: 600, height: 800),
            CGRect(x: 600, y: 25, width: 600, height: 800),
        ])
    }

    func testFourIsTwoByTwo() {
        let cells = gridLayout(count: 4, in: area)
        XCTAssertEqual(cells.count, 4)
        XCTAssertEqual(cells[0], CGRect(x: 0, y: 25, width: 600, height: 400))
        XCTAssertEqual(cells[1], CGRect(x: 600, y: 25, width: 600, height: 400))
        XCTAssertEqual(cells[2], CGRect(x: 0, y: 425, width: 600, height: 400))
        XCTAssertEqual(cells[3], CGRect(x: 600, y: 425, width: 600, height: 400))
    }

    func testSevenIsThreeColsLastRowStretched() {
        let cells = gridLayout(count: 7, in: area)
        XCTAssertEqual(cells.count, 7)
        // rows of 3,3,1. row height 800/3.
        let rowH = 800.0 / 3.0
        XCTAssertEqual(cells[0].width, 400, accuracy: 0.001)
        XCTAssertEqual(cells[0].height, rowH, accuracy: 0.001)
        XCTAssertEqual(cells[6].minX, 0)
        XCTAssertEqual(cells[6].width, 1200)
        XCTAssertEqual(cells[6].minY, 25 + 2 * rowH, accuracy: 0.001)
    }

    func testTwelveIsFourByThree() {
        let cells = gridLayout(count: 12, in: area)
        XCTAssertEqual(cells.count, 12)
        XCTAssertEqual(cells[0].width, 300)
        XCTAssertEqual(cells[0].height, 800.0 / 3.0, accuracy: 0.001)
        XCTAssertEqual(cells[11].maxX, 1200, accuracy: 0.001)
        XCTAssertEqual(cells[11].maxY, 825, accuracy: 0.001)
    }

    func testCellsDoNotOverlap() {
        for n in 1...16 {
            let cells = gridLayout(count: n, in: area)
            for i in 0..<cells.count {
                for j in (i + 1)..<cells.count {
                    XCTAssertFalse(cells[i].insetBy(dx: 0.01, dy: 0.01).intersects(cells[j]), "n=\(n) cells \(i),\(j) overlap")
                }
            }
        }
    }
}
```

- [ ] **Step 4: Run tests, verify they fail**

Run: `cd ~/Documents/gridtile && swift test 2>&1 | tail -5`
Expected: compile error, `cannot find 'gridLayout' in scope`

- [ ] **Step 5: Implement Grid.swift**

`Sources/GridTileCore/Grid.swift`:
```swift
import CoreGraphics

/// Splits `area` into `count` cells. cols = ceil(sqrt(count)), rows = ceil(count/cols).
/// Full rows have `cols` cells. The last row stretches its cells to fill the width.
/// Cells are returned left-to-right, top-to-bottom, in CG coordinates (y down).
public func gridLayout(count: Int, in area: CGRect) -> [CGRect] {
    guard count > 0 else { return [] }
    let cols = Int(Double(count).squareRoot().rounded(.up))
    let rows = (count + cols - 1) / cols
    let rowHeight = area.height / CGFloat(rows)
    var cells: [CGRect] = []
    cells.reserveCapacity(count)
    for row in 0..<rows {
        let inRow = min(cols, count - row * cols)
        let width = area.width / CGFloat(inRow)
        for col in 0..<inRow {
            cells.append(CGRect(
                x: area.minX + CGFloat(col) * width,
                y: area.minY + CGFloat(row) * rowHeight,
                width: width,
                height: rowHeight))
        }
    }
    return cells
}
```

- [ ] **Step 6: Run tests, verify they pass**

Run: `cd ~/Documents/gridtile && swift test 2>&1 | tail -5`
Expected: `Executed 7 tests, with 0 failures`

- [ ] **Step 7: Commit**

```bash
cd ~/Documents/gridtile && git add -A && git commit -m "feat: package skeleton and grid layout with tests"
```

---

### Task 2: Window enumeration and AX apply

**Files:**
- Create: `Sources/GridTile/Windows.swift`

No unit tests (needs a live window server and Accessibility permission). Verified in Task 4.

- [ ] **Step 1: Write Windows.swift**

```swift
import AppKit
import ApplicationServices

struct VisibleWindow {
    let pid: pid_t
    let frame: CGRect      // CG coordinates
    let ax: AXUIElement
}

private let excludedOwners: Set<String> = [
    "Dock", "WindowServer", "Window Server", "Control Center",
    "Notification Center", "Spotlight", "GridTile",
]

/// Converts an AppKit rect (origin bottom-left of primary display, y up)
/// to CG coordinates (origin top-left of primary display, y down).
func cgRect(of nsRect: CGRect) -> CGRect {
    let primaryHeight = NSScreen.screens[0].frame.height
    return CGRect(x: nsRect.minX, y: primaryHeight - nsRect.maxY, width: nsRect.width, height: nsRect.height)
}

/// Windows currently on screen (current Space, not minimized, not hidden) whose center
/// lies on `screen`. Sorted top-to-bottom, then left-to-right.
func visibleWindows(on screen: NSScreen) -> [VisibleWindow] {
    let target = cgRect(of: screen.frame)
    let options: CGWindowListOption = [.optionOnScreenOnly, .excludeDesktopElements]
    guard let list = CGWindowListCopyWindowInfo(options, kCGNullWindowID) as? [[String: Any]] else { return [] }

    var result: [VisibleWindow] = []
    for info in list {
        guard (info[kCGWindowLayer as String] as? Int) == 0,
              (info[kCGWindowAlpha as String] as? Double ?? 1) > 0,
              let pid = info[kCGWindowOwnerPID as String] as? pid_t,
              let owner = info[kCGWindowOwnerName as String] as? String,
              !excludedOwners.contains(owner),
              let boundsDict = info[kCGWindowBounds as String] as? NSDictionary,
              let frame = CGRect(dictionaryRepresentation: boundsDict),
              frame.width > 50, frame.height > 50,
              target.contains(CGPoint(x: frame.midX, y: frame.midY)),
              let ax = axWindow(pid: pid, matching: frame)
        else { continue }
        result.append(VisibleWindow(pid: pid, frame: frame, ax: ax))
    }
    return result.sorted { a, b in
        a.frame.minY != b.frame.minY ? a.frame.minY < b.frame.minY : a.frame.minX < b.frame.minX
    }
}

private func axWindow(pid: pid_t, matching frame: CGRect) -> AXUIElement? {
    let app = AXUIElementCreateApplication(pid)
    var value: CFTypeRef?
    guard AXUIElementCopyAttributeValue(app, kAXWindowsAttribute as CFString, &value) == .success,
          let windows = value as? [AXUIElement] else { return nil }
    return windows.first { w in
        guard let f = axFrame(w) else { return false }
        return abs(f.minX - frame.minX) <= 2 && abs(f.minY - frame.minY) <= 2
            && abs(f.width - frame.width) <= 2 && abs(f.height - frame.height) <= 2
    }
}

private func axFrame(_ w: AXUIElement) -> CGRect? {
    var posRef: CFTypeRef?
    var sizeRef: CFTypeRef?
    guard AXUIElementCopyAttributeValue(w, kAXPositionAttribute as CFString, &posRef) == .success,
          AXUIElementCopyAttributeValue(w, kAXSizeAttribute as CFString, &sizeRef) == .success,
          let posVal = posRef, let sizeVal = sizeRef,
          CFGetTypeID(posVal) == AXValueGetTypeID(), CFGetTypeID(sizeVal) == AXValueGetTypeID()
    else { return nil }
    var pos = CGPoint.zero
    var size = CGSize.zero
    AXValueGetValue(posVal as! AXValue, .cgPoint, &pos)
    AXValueGetValue(sizeVal as! AXValue, .cgSize, &size)
    return CGRect(origin: pos, size: size)
}

/// Sets position then size. Apps with minimum sizes keep whatever size they accept.
func apply(frame: CGRect, to w: AXUIElement) {
    var pos = frame.origin
    var size = frame.size
    if let v = AXValueCreate(.cgPoint, &pos) {
        AXUIElementSetAttributeValue(w, kAXPositionAttribute as CFString, v)
    }
    if let v = AXValueCreate(.cgSize, &size) {
        AXUIElementSetAttributeValue(w, kAXSizeAttribute as CFString, v)
    }
}
```

- [ ] **Step 2: Build**

Run: `cd ~/Documents/gridtile && swift build 2>&1 | tail -3`
Expected: `Build complete!` (warnings about unused functions are fine)

- [ ] **Step 3: Commit**

```bash
cd ~/Documents/gridtile && git add -A && git commit -m "feat: enumerate on-screen windows and apply AX frames"
```

---

### Task 3: Hotkey and app entry

**Files:**
- Create: `Sources/GridTile/Hotkey.swift`
- Replace: `Sources/GridTile/main.swift`

- [ ] **Step 1: Write Hotkey.swift**

```swift
import Carbon

/// Global hotkey via Carbon. Keep the instance alive for as long as the hotkey should work.
final class Hotkey {
    private var hotKeyRef: EventHotKeyRef?
    private let handler: () -> Void

    init(keyCode: UInt32, modifiers: UInt32, handler: @escaping () -> Void) {
        self.handler = handler
        var spec = EventTypeSpec(eventClass: OSType(kEventClassKeyboard), eventKind: UInt32(kEventHotKeyPressed))
        InstallEventHandler(GetApplicationEventTarget(), { _, _, userData in
            guard let userData else { return noErr }
            Unmanaged<Hotkey>.fromOpaque(userData).takeUnretainedValue().handler()
            return noErr
        }, 1, &spec, Unmanaged.passUnretained(self).toOpaque(), nil)
        let id = EventHotKeyID(signature: 0x4752_4454 /* "GRDT" */, id: 1)
        RegisterEventHotKey(keyCode, modifiers, id, GetApplicationEventTarget(), 0, &hotKeyRef)
    }

    deinit {
        if let ref = hotKeyRef { UnregisterEventHotKey(ref) }
    }
}
```

- [ ] **Step 2: Write main.swift (replaces placeholder)**

```swift
import AppKit
import Carbon
import ServiceManagement
import GridTileCore

func tileNow() {
    let mouse = NSEvent.mouseLocation
    let screen = NSScreen.screens.first { $0.frame.contains(mouse) } ?? NSScreen.main ?? NSScreen.screens[0]
    let windows = visibleWindows(on: screen)
    let cells = gridLayout(count: windows.count, in: cgRect(of: screen.visibleFrame))
    for (window, cell) in zip(windows, cells) {
        apply(frame: cell, to: window.ax)
    }
}

final class AppDelegate: NSObject, NSApplicationDelegate {
    private var statusItem: NSStatusItem!
    private var hotkey: Hotkey!

    func applicationDidFinishLaunching(_ notification: Notification) {
        let options = [kAXTrustedCheckOptionPrompt.takeUnretainedValue() as String: true] as CFDictionary
        let trusted = AXIsProcessTrustedWithOptions(options)

        statusItem = NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)
        statusItem.button?.title = "⊞"

        let menu = NSMenu()

        let tileItem = NSMenuItem(title: "Tile Now  ⌃⌥⌘T", action: #selector(tile), keyEquivalent: "")
        tileItem.target = self
        menu.addItem(tileItem)

        if !trusted {
            menu.addItem(NSMenuItem(title: "Accessibility: not granted", action: nil, keyEquivalent: ""))
        }

        let loginItem = NSMenuItem(title: "Launch at Login", action: #selector(toggleLogin(_:)), keyEquivalent: "")
        loginItem.target = self
        loginItem.state = SMAppService.mainApp.status == .enabled ? .on : .off
        menu.addItem(loginItem)

        menu.addItem(.separator())
        menu.addItem(NSMenuItem(title: "Quit", action: #selector(NSApplication.terminate(_:)), keyEquivalent: "q"))
        statusItem.menu = menu

        hotkey = Hotkey(keyCode: UInt32(kVK_ANSI_T), modifiers: UInt32(controlKey | optionKey | cmdKey)) {
            tileNow()
        }
    }

    @objc private func tile() { tileNow() }

    @objc private func toggleLogin(_ item: NSMenuItem) {
        do {
            if SMAppService.mainApp.status == .enabled {
                try SMAppService.mainApp.unregister()
                item.state = .off
            } else {
                try SMAppService.mainApp.register()
                item.state = .on
            }
        } catch {
            NSLog("Launch at Login failed: \(error)")
        }
    }
}

let app = NSApplication.shared
let delegate = AppDelegate()
app.delegate = delegate
app.setActivationPolicy(.accessory)
app.run()
```

- [ ] **Step 3: Build and run tests**

Run: `cd ~/Documents/gridtile && swift build 2>&1 | tail -3 && swift test 2>&1 | tail -2`
Expected: `Build complete!` and `Executed 7 tests, with 0 failures`

- [ ] **Step 4: Commit**

```bash
cd ~/Documents/gridtile && git add -A && git commit -m "feat: menu bar app with global hotkey"
```

---

### Task 4: App bundle, install, manual verification

**Files:**
- Create: `Info.plist`
- Create: `build.sh`
- Create: `README.md`

- [ ] **Step 1: Write Info.plist**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleExecutable</key>
    <string>GridTile</string>
    <key>CFBundleIdentifier</key>
    <string>com.local.gridtile</string>
    <key>CFBundleName</key>
    <string>GridTile</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSMinimumSystemVersion</key>
    <string>13.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSHighResolutionCapable</key>
    <true/>
</dict>
</plist>
```

- [ ] **Step 2: Write build.sh**

```bash
#!/bin/bash
# Builds GridTile.app and installs it to /Applications.
set -euo pipefail
cd "$(dirname "$0")"

swift build -c release

APP=build/GridTile.app
rm -rf "$APP"
mkdir -p "$APP/Contents/MacOS"
cp .build/release/GridTile "$APP/Contents/MacOS/GridTile"
cp Info.plist "$APP/Contents/Info.plist"
codesign --force --sign - "$APP"

pkill -x GridTile 2>/dev/null || true
rm -rf /Applications/GridTile.app
cp -R "$APP" /Applications/
echo "Installed /Applications/GridTile.app"
echo "Ad-hoc signature changes every build: re-grant Accessibility in System Settings > Privacy & Security > Accessibility after each install."
```

Run: `chmod +x ~/Documents/gridtile/build.sh`

- [ ] **Step 3: Write README.md**

```markdown
# GridTile

Ctrl+Opt+Cmd+T tiles every visible window on the display under the mouse into an auto grid.

- 1 window: full screen. 2: side by side. 4: 2x2. 7: 3+3+1 (last row stretched). 12: 4x3.
- Only windows on the current Space and display. Minimized and hidden windows are ignored.

## Install

    ./build.sh
    open /Applications/GridTile.app

Grant Accessibility when prompted (System Settings > Privacy & Security > Accessibility).
Re-grant after every rebuild (ad-hoc signature changes).

## Test

    swift test
```

- [ ] **Step 4: Build, install, launch**

Run: `cd ~/Documents/gridtile && ./build.sh && open /Applications/GridTile.app`
Expected: `Installed /Applications/GridTile.app`, then a `⊞` item appears in the menu bar and macOS prompts for Accessibility.

- [ ] **Step 5: Manual verification**

1. Grant Accessibility for GridTile. Quit and relaunch: `pkill -x GridTile; open /Applications/GridTile.app`.
2. Open 7 windows (e.g. `for i in 1 2 3 4 5 6 7; do open -na TextEdit; done`, then Cmd+N in each if needed).
3. Press Ctrl+Opt+Cmd+T. Expected: 3 windows top row, 3 middle, 1 full-width bottom.
4. Close 3 windows, press again. Expected: 2x2.
5. Minimize 1, press again. Expected: 3 windows tile (2 top, 1 stretched bottom), minimized one untouched.
6. Menu bar: click `⊞`, Tile Now works, Launch at Login toggles a checkmark, Quit exits.

- [ ] **Step 6: Commit**

```bash
cd ~/Documents/gridtile && git add -A && git commit -m "feat: app bundle build script and README"
```
